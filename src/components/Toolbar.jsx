import React from 'react';
import { HStack, Button } from '@chakra-ui/react';

const Toolbar = () => {
  return (
    <HStack as="nav" bg="gray.200" color="gray.200" p="2">
      {/* Botões adicionais podem ser adicionados aqui */}
    </HStack>
  );
};

export default Toolbar;
